name: blastn_mlva
channels:
  - conda-forge
  - bioconda
dependencies:
  - blast=2.13.0
  - gsl=2.7
  - pandas=1.1.5
  - pip=20.0.2
  - python=3.6.15
  - readline=8.1.2
  - pip:
    - termcolor==1.1.0
    - tqdm
    - rich
# prefix: /path/to/envs/blastn_mlva
